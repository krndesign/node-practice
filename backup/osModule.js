const os = require('os')

console.log(os.type()) // detact the operating system
console.log(os.platform()) // detact the platform
console.log(os.arch()) // detact the architecture
console.log(os.hostname()) // detact the hostname
console.log(os.release()) // detact the release
console.log(os.uptime()) // detact the uptime
console.log(os.userInfo()) // detact the user info
console.log(os.cpus()) // detact the cpus
console.log(os.freemem()) // detact the free memory
console.log(os.totalmem()) // detact the total memory